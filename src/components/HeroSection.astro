---
import { ArrowRight, BadgeCheck } from '@lucide/astro';
import { Image } from 'astro:assets';
import portraitImage from '../assets/images/nicodev.webp';
---

<section id="site-hero" class="hero-home section-y border-b border-border bg-white overflow-hidden">
	<div class="shell-layout">
		<div class="hero-home__grid">
			<div class="hero-home__copy">
				<p class="type-label-mono text-primary-foreground">Sites web pour psychologues et professionnels du soin</p>
				<h1 class="hero-home__title text-balance text-foreground">
					Un site internet clair pour présenter votre pratique de soin.
				</h1>
				<p class="hero-home__lead text-pretty text-muted">
					Création et refonte de site internet pour psychologues, psychomotriciens et professions proches :
					présenter votre pratique, rassurer vos futurs patients et orienter vers le rendez-vous. Psychologue,
					ancien psychomotricien et développeur web, je connais les enjeux du soin — vous recevez vos patients,
					je m'occupe du site et de votre visibilité.
				</p>

				<div class="hero-home__actions">
					<a
						href="/diagnostic-gratuit/"
						data-suppress-header-diagnostic-when-visible
						class="btn-primary-solid inline-flex items-center justify-center gap-2 rounded-xl px-8 py-4"
					>
						Demander un diagnostic gratuit
						<ArrowRight class="h-5 w-5" stroke-width={1.75} aria-hidden="true" />
					</a>
					<a
						href="/tarifs/"
						class="btn-outline-terracotta inline-flex items-center justify-center rounded-xl border border-border bg-transparent px-8 py-4 font-bold text-foreground hover:bg-surface-alt"
					>
						Voir les tarifs
					</a>
				</div>

				<p class="type-label-mono hero-home__price flex items-center gap-1.5 text-muted">
					<BadgeCheck class="h-4 w-4 shrink-0 text-primary-foreground" stroke-width={1.75} aria-hidden="true" />
					À partir de 29&nbsp;€/mois, tout inclus
				</p>

				<p class="hero-home__trust text-sm text-muted">
					<span class="text-foreground/85">Une spécialisation historique auprès des psychologues depuis 2016</span>
					<span class="mx-1.5 text-muted/45" aria-hidden="true">·</span>
					<a
						href="#temoignages"
						class="font-medium text-foreground underline decoration-primary/40 underline-offset-4 transition-colors hover:text-primary-foreground hover:decoration-primary-foreground"
					>
						Avis de praticiens accompagnés
					</a>
				</p>
			</div>

			<div class="hero-home__visual">
				<div class="hero-home__portrait">
					<Image
						src={portraitImage}
						alt="Nicolas Devaux — psychologue, ancien psychomotricien et développeur web"
						class="h-full w-full object-cover"
						width={640}
						height={800}
						loading="eager"
						fetchpriority="high"
						decoding="async"
					/>
				</div>
				<aside class="hero-home__card" aria-label="Présentation">
					<p class="font-heading text-xl font-semibold text-primary-foreground">Nicolas Devaux</p>
					<p class="mt-1 text-sm leading-relaxed text-muted">
						Psychologue, ancien psychomotricien &amp; développeur web avec 20 ans d'expérience.
					</p>
				</aside>
			</div>
		</div>
	</div>
</section>

<style>
	.hero-home {
		position: relative;
		overflow: hidden;
	}

	.hero-home__grid {
		display: grid;
		grid-template-columns: 1fr;
		align-items: center;
		gap: 2.5rem;
	}

	@media (min-width: 768px) {
		.hero-home__grid {
			grid-template-columns: minmax(0, 1.05fr) minmax(0, 0.95fr);
			gap: 3rem;
		}
	}

	@media (min-width: 1024px) {
		.hero-home__grid {
			gap: 4rem;
		}
	}

	.hero-home__copy {
		text-align: center;
	}

	@media (min-width: 768px) {
		.hero-home__copy {
			text-align: left;
		}
	}

	.hero-home__title {
		margin-top: 0.75rem;
		font-family: var(--font-heading), ui-serif, Georgia, serif;
		font-size: clamp(2.125rem, 1.35vw + 1.65rem, 3.25rem);
		font-weight: 600;
		line-height: 1.12;
		letter-spacing: -0.02em;
	}

	.hero-home__lead {
		margin-top: 1.25rem;
		max-width: 42rem;
		font-size: clamp(1rem, 0.25vw + 0.96rem, 1.125rem);
		line-height: 1.55;
	}

	@media (min-width: 768px) {
		.hero-home__lead {
			margin-inline: 0;
		}
	}

	.hero-home__actions {
		margin-top: 1.75rem;
		display: flex;
		flex-direction: column;
		align-items: center;
		gap: 0.75rem;
	}

	@media (min-width: 640px) {
		.hero-home__actions {
			flex-direction: row;
			flex-wrap: wrap;
			justify-content: center;
		}
	}

	@media (min-width: 768px) {
		.hero-home__actions {
			justify-content: flex-start;
		}
	}

	.hero-home__price {
		margin-top: 1rem;
		justify-content: center;
	}

	@media (min-width: 768px) {
		.hero-home__price {
			justify-content: flex-start;
		}
	}

	.hero-home__trust {
		margin-top: 1rem;
	}

	.hero-home__visual {
		position: relative;
		width: 100%;
		max-width: 28rem;
		margin-inline: auto;
	}

	@media (min-width: 768px) {
		.hero-home__visual {
			max-width: none;
			margin-inline: 0;
		}
	}

	.hero-home__portrait {
		aspect-ratio: 4 / 5;
		overflow: hidden;
		border-radius: var(--radius-lg);
		border: 1px solid var(--color-border);
		box-shadow: var(--shadow-lg);
	}

	.hero-home__card {
		position: absolute;
		bottom: -1.25rem;
		left: -0.5rem;
		max-width: 16rem;
		padding: 1.25rem 1.5rem;
		border-radius: var(--radius-lg);
		border: 1px solid var(--color-border);
		background: var(--color-neutral-0);
		box-shadow: var(--shadow-md);
	}

	@media (min-width: 640px) {
		.hero-home__card {
			left: -1.25rem;
			max-width: 18rem;
		}
	}
</style>
