---
---
<section id="lead-form" class="py-16 bg-surface-alt">
	<div class="max-w-3xl mx-auto px-4 sm:px-6 lg:px-8">
		<div class="bg-background rounded-2xl shadow-lg border border-border p-6 md:p-8">
			<h2 class="text-3xl font-bold text-foreground">Recevoir votre proposition personnalisée en 48h</h2>
			<p class="text-muted mt-3">Décrivez votre contexte en quelques mots. Je vous recommande l'offre la plus adaptée à votre situation.</p>

			<div id="home-form-success" class="hidden mt-6 bg-surface border border-border rounded-lg p-4 text-foreground">
				Message envoyé. Je reviens vers vous rapidement.
			</div>

			<form id="home-contact-form" name="lead" method="POST" data-netlify="true" netlify-honeypot="bot-field" class="mt-6 space-y-4">
				<input type="hidden" name="form-name" value="lead" />
				<p class="hidden" aria-hidden="true">
					<label for="bot-field">Ne pas remplir</label>
					<input id="bot-field" name="bot-field" type="text" tabindex="-1" autocomplete="off" />
				</p>
				<div class="grid grid-cols-1 md:grid-cols-2 gap-4">
					<div>
						<label for="name" class="block text-sm font-medium text-muted mb-1">Nom et prénom *</label>
						<input id="name" name="name" required class="w-full border border-border rounded-lg px-3 py-2 focus:ring-2 focus:ring-primary focus:border-primary outline-none" />
					</div>
					<div>
						<label for="email" class="block text-sm font-medium text-muted mb-1">Email *</label>
						<input id="email" name="email" type="email" required class="w-full border border-border rounded-lg px-3 py-2 focus:ring-2 focus:ring-primary focus:border-primary outline-none" />
					</div>
				</div>
				<div>
					<label for="formule" class="block text-sm font-medium text-muted mb-1">Formule souhaitée *</label>
					<select id="formule" name="formule" required class="w-full border border-border rounded-lg px-3 py-2 focus:ring-2 focus:ring-primary focus:border-primary outline-none">
						<option value="">Sélectionnez une formule</option>
						<option value="forfait">Forfait Création - 490€ (sans abonnement)</option>
						<option value="vitrine">Pack Site Vitrine - 59€/mois</option>
						<option value="blog">Pack Site + Blog - 89€/mois</option>
						<option value="indecis">Je ne sais pas encore</option>
					</select>
				</div>
				<div>
					<label for="message" class="block text-sm font-medium text-muted mb-1">Votre besoin *</label>
					<textarea id="message" name="message" rows="5" required class="w-full border border-border rounded-lg px-3 py-2 focus:ring-2 focus:ring-primary focus:border-primary outline-none" placeholder="Type de pratique, délai, objectifs, éléments déjà disponibles..."></textarea>
				</div>
				<button id="home-submit-btn" type="submit" class="w-full bg-primary text-on-primary py-3 rounded-lg font-semibold hover:opacity-90 transition-colors disabled:opacity-60 disabled:cursor-not-allowed">
					Recevoir ma proposition
				</button>
			</form>
		</div>
	</div>
</section>

<script>
	const homeForm = document.getElementById('home-contact-form');
	const homeSubmit = document.getElementById('home-submit-btn');
	const homeSuccess = document.getElementById('home-form-success');

	if (homeForm && homeSubmit && homeSuccess) {
		homeForm.addEventListener('submit', async (event) => {
			event.preventDefault();
			homeSubmit.setAttribute('disabled', 'true');
			homeSubmit.textContent = 'Envoi en cours...';

			const form = homeForm as HTMLFormElement;
			const formData = new FormData(form);
			const params = new URLSearchParams();
			formData.forEach((value, key) => params.append(key, typeof value === 'string' ? value : (value as File).name));
			const response = await fetch('/', {
				method: 'POST',
				body: params.toString(),
				headers: { 'Content-Type': 'application/x-www-form-urlencoded' }
			});

			if (response.ok) {
				form.classList.add('hidden');
				homeSuccess.classList.remove('hidden');
			} else {
				homeSubmit.removeAttribute('disabled');
				homeSubmit.textContent = 'Recevoir ma proposition';
				alert('Une erreur est survenue. Veuillez réessayer.');
			}
		});
	}
</script>
