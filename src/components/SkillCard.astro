---
export interface Props {
	title: string;
	description: string;
	centered?: boolean;
}

const { title, description, centered = true } = Astro.props;
---

<div class={`rounded-lg border border-border bg-background p-6 shadow-sm ${centered ? 'text-center' : ''}`}>
	<div class={`w-16 h-16 bg-surface rounded-full flex items-center justify-center ${centered ? 'mx-auto' : ''} mb-4`}>
		<div class="text-foreground">
			<slot name="icon" />
		</div>
	</div>
	<h3 class="text-lg font-semibold text-foreground mb-2">{title}</h3>
	<p class={`text-muted ${centered ? 'text-sm' : ''}`}>
		{description}
	</p>
</div>
